// WAP to calculate the area of the rectange by taking input for the user 
#include<stdio.h>
int main(){
    int l,b,area;
    l;
    printf("Enter the length of the rectangle :");
    scanf("%d",&l);
    b;
    printf("Enter the breadth of the rectangle :");
    scanf("%d",&b);
    area = l*b;
    printf("The area of the rectanlge = %d",area);
    return 0;
}